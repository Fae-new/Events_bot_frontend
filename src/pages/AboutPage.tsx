import React from "react";

const AboutPage: React.FC = () => (
  <div style={{ maxWidth: 500, margin: "2rem auto", textAlign: "left" }}>
    <h2>About</h2>
    <p>
      This is a simple React chat app demo using Vite, TypeScript, and React
      Router.
    </p>
  </div>
);

export default AboutPage;
